import React from 'react';
import Logo from '../Logo';
import NavBar from '../NavBar';
import Player from '../Player';

export default () => (
  <div>
    <Logo />
    <NavBar />
    <Player />
  </div>
);
