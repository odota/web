import React from "react";
import { LoadingOverlayUpper30 } from "../LoadingOverlay";

const DurationsSkeleton = (props: any) => (
  <LoadingOverlayUpper30 text="Loading durations..." />
);

export default DurationsSkeleton;
