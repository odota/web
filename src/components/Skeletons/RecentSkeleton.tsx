import React from "react";
import { LoadingOverlayUpper30 } from "../LoadingOverlay";

const RecentSkeleton = (props: any) => (
  <LoadingOverlayUpper30 text="Loading recent games..." />
);

export default RecentSkeleton;
