import React from "react";
import { LoadingOverlayUpper30 } from "../LoadingOverlay";

const DistributionsSkeleton = (props: any) => (
  <LoadingOverlayUpper30 text="Loading distribution curve..." />
);

export default DistributionsSkeleton;
