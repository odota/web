import { prism } from '.';
import { isNonPositive } from './NonPositive';
import { isNonZero } from './NonZero';
/**
 * @since 0.2.0
 */
export var isNegative = function (n) { return isNonZero(n) && isNonPositive(n); };
/**
 * @since 0.2.0
 */
export var prismNegative = prism(isNegative);
