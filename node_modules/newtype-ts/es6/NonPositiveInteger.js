import { prism } from '.';
import { isInteger } from './Integer';
import { isNonPositive } from './NonPositive';
/**
 * @since 0.2.0
 */
export var isNonPositiveInteger = function (n) { return isNonPositive(n) && isInteger(n); };
/**
 * @since 0.2.0
 */
export var prismNonPositiveInteger = prism(isNonPositiveInteger);
