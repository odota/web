import { prism } from '.';
import { isNonNegativeInteger } from './NonNegativeInteger';
import { isNonZero } from './NonZero';
/**
 * @since 0.2.0
 */
export var isPositiveInteger = function (n) { return isNonZero(n) && isNonNegativeInteger(n); };
/**
 * @since 0.2.0
 */
export var prismPositiveInteger = prism(isPositiveInteger);
