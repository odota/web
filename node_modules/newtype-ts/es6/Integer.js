/**
 * @since 0.2.0
 */
import { prism } from '.';
/**
 * @since 0.2.0
 */
export var isInteger = function (n) { return Number.isInteger(n); };
/**
 * @since 0.2.0
 */
export var prismInteger = prism(isInteger);
