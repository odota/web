import { LocationState } from 'history';
import { RouteComponentProps, StaticContext } from 'react-router';
export default function useRouter<P extends {
    [K in keyof P]?: string;
} = {}, C extends StaticContext = StaticContext, S = LocationState>(): RouteComponentProps<P, C, S>;
